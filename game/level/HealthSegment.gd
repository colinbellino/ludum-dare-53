extends TextureRect

func _ready():
	# (material as ShaderMaterial).set_shader_parameter("mask_offset", Vector2i(4, 0))
	# await get_tree().create_timer(1.0).timeout
	# (material as ShaderMaterial).set_shader_parameter("mask_offset", Vector2i(3, 0))
	# await get_tree().create_timer(1.0).timeout
	# (material as ShaderMaterial).set_shader_parameter("mask_offset", Vector2i(2, 0))
	# await get_tree().create_timer(1.0).timeout
	# (material as ShaderMaterial).set_shader_parameter("mask_offset", Vector2i(1, 0))
	# await get_tree().create_timer(1.0).timeout
	# (material as ShaderMaterial).set_shader_parameter("mask_offset", Vector2i(0, 0))
	pass
